Airflow docker-compose for DEAP demo.

Start:
    cd orchestration/airflow
    docker compose up -d

Visit Airflow UI: http://localhost:8080 (login airflow/airflow)

The 'dags' folder already contains a copy of the deap project (dags/deap).
